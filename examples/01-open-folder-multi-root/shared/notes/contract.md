# Shared Contract

This folder exists to make the multi-root workspace feel more like a real monorepo slice.

- `Client` owns rendering scripts.
- `Service` owns the service launch/debug surface.
- `Shared Notes` holds documentation and helper tooling.

